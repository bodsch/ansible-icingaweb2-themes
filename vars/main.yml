---

icingaweb_themes_local_tmp_directory: "{{
  lookup('env', 'CUSTOM_LOCAL_TMP_DIRECTORY') |
  default(lookup('env', 'HOME') ~ '/.cache/ansible/icingaweb-themes', true) }}"

icingaweb_themes_remote_tmp_directory: "/tmp/icingaweb-themes"

# entspricht dem owner
icingaweb_user: ""
# entspricht der grupp
icingaweb_group: ""

...
